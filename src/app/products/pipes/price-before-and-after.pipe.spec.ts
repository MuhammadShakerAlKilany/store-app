import { PriceBeforeAndAfterPipe } from './price-before-and-after.pipe';

describe('PriceBeforeAndAfterPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceBeforeAndAfterPipe();
    expect(pipe).toBeTruthy();
  });
});
